To start the platform:

Make sure you have installed Vue.js and local MySQL server.

In the back-end directory, start the Python program:
```bash
python app.py
```

In the front-end directory, install the necessary libraries:

```bash
npm install
```

Run the front-end program:

```bash
npm run serve
```

In the browser, open localhost.
